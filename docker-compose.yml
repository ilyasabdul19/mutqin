services:
  db:
    image: postgres:16-alpine
    container_name: mutqin-db
    environment:
      POSTGRES_USER: mutqin
      POSTGRES_PASSWORD: mutqin
      POSTGRES_DB: mutqin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mutqin -d mutqin"]
      interval: 2s
      timeout: 2s
      retries: 30
    networks:
      - mutqin

  redis:
    image: redis:7-alpine
    container_name: mutqin-redis
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 1s
      retries: 30
    networks:
      - mutqin

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    image: mutqin-api:dev
    container_name: mutqin-api
    environment:
      DATABASE_URL: postgres://mutqin:mutqin@db:5432/mutqin?sslmode=disable
      APP_DATABASE_URL: postgres://mutqin_app:mutqin_app@db:5432/mutqin?sslmode=disable
      JWT_SECRET: dev-secret
      BASE_HOST: localhost
      PORT: "8080"
    depends_on:
      db:
        condition: service_healthy
    labels:
      - traefik.enable=true
      - traefik.docker.network=mutqin
      # API router — matches /api on any host. Higher priority than web so the
      # path prefix wins over web's catch-all.
      - traefik.http.routers.api.rule=PathPrefix(`/api`)
      - traefik.http.routers.api.entrypoints=web
      - traefik.http.routers.api.priority=100
      - traefik.http.services.api.loadbalancer.server.port=8080
    networks:
      - mutqin

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: mutqin-web:dev
    container_name: mutqin-web
    labels:
      - traefik.enable=true
      - traefik.docker.network=mutqin
      # Web router — matches everything else. Lower priority than api.
      - traefik.http.routers.web.rule=PathPrefix(`/`)
      - traefik.http.routers.web.entrypoints=web
      - traefik.http.routers.web.priority=10
      - traefik.http.services.web.loadbalancer.server.port=80
    networks:
      - mutqin

  traefik:
    image: traefik:v3.1
    container_name: mutqin-traefik
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "80:80"
      - "8081:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./infra/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
    depends_on:
      - api
      - web
    networks:
      - mutqin

volumes:
  pgdata:

networks:
  mutqin:
    name: mutqin
